<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ef.mappers.AccessLogMapper">
    <insert id="insertAccessLog" parameterType="com.ef.model.AccessLog">
        INSERT INTO access_log (accessTime, ipAddress, method, status, client)
        VALUES (#{logData.accessTime}, #{logData.ipAddress}, #{logData.method}, #{logData.status}, #{logData.client})
    </insert>
</mapper>